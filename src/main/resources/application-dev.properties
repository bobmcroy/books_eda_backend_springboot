spring.config.activate.on-profile=dev

app.env=dev
app.seed=true

# Use cloud region from base; credentials via AWS_PROFILE or env vars
# Example: AWS_PROFILE=dev ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
